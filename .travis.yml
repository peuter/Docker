language: generic
sudo: required
services:
    - docker
    
script: ls ./
#before_deploy:
#    - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin   
deploy:
  provider: script
  script: cd ./CometVisuBase && sudo ./hooks/build --noPush